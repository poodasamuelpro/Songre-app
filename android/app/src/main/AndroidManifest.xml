<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- CRITIQUE : Permission Internet requise pour Supabase + Google Fonts -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- CAMERA : requise par mobile_scanner pour le scan QR (ScanQrScreen) -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- POST_NOTIFICATIONS : requise sur Android 13+ (API 33+) pour Firebase Messaging -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Lanceur principal -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- [Fix #1a] Deep linking — schéma personnalisé songre://
                 Intercepte les liens de réinitialisation de mot de passe :
                 songre://reset-password?access_token=xxx&type=recovery
                 Supabase redirige vers ce schéma depuis l'email de reset. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="songre"/>
            </intent-filter>

            <!-- Deep linking HTTPS — https://songre.vercel.app/reset-password
                 Fallback web : si l'app n'est pas installée, le navigateur
                 ouvre la page Vercel. Si elle est installée, Android propose
                 d'ouvrir l'application. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="songre.vercel.app"
                    android:pathPrefix="/reset-password"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- [P5] Active la transmission des deep links au moteur Flutter / GoRouter.
             Sans cette meta-data, l'URI reçue via un Intent ACTION_VIEW (ex: lien email
             reset-password) est ignorée et l'app démarre sur la route initiale au lieu
             de la route correspondant au lien. Requis pour go_router >= 6.0. -->
        <meta-data
            android:name="flutter.deeplinking.enabled"
            android:value="true" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>









